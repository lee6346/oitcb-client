Note:

The core folder contains all service providers to be injected at the root module
It also contains root level components that ONLY be used at the root level
The index.ts file groups all export services/components into one

Will later regroup the services into subdirectories

